// Core module: the platform-independent game logic + render/input layer. No external assets.
// Add extension deps (e.g. gdx-box2d, gdx-freetype) here when the task calls for them.

dependencies {
    implementation "com.badlogicgames.gdx:gdx:$gdxVersion"
}
